[project]
name = "recime-probe"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["yt-dlp", "google-genai", "pydantic>=2", "python-decouple>=3.8"]

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
